# Adapters

`adapters/` 存放工具适配说明。核心规范必须保持工具中立；适配层只负责把这些规范映射到具体工具。

## 已提供适配

- `claude-code.md`
- `codex.md`
- `workbuddy.md`

## 适配原则

- 不复制核心规范到工具私有配置中。
- 工具入口必须指向 `AGENTS.md`。
- 工具命令必须映射到 `commands/` 中的标准动作。
- 成熟工作流可以封装为工具能力，但源规范仍保留在仓库。

